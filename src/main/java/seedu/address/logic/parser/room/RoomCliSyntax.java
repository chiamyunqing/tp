package seedu.address.logic.parser.room;

import seedu.address.logic.parser.Prefix;

/**
 * Contains Command Line Interface (CLI) syntax definitions common to multiple commands for room
 */
public class RoomCliSyntax {

    /* Prefix definitions */
    public static final Prefix PREFIX_ROOM = new Prefix("r/");

}
